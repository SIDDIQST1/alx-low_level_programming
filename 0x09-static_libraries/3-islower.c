#include "main.h"
/**
 * _islower - checks for lowercase character
 * @c: the character to check
 * Return: 1 if int c is a lowercase, 0 else
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
